svn  集中式  版本管理工具：版本库是集中存放在中央服务器的      必须联网才能工作
Git 分布式   版本管理工具：每个人的电脑上都是一个完整的版本库  不必联网

Git 的学习笔记

添加文件：需要2步
1. git add '**.html' // 可反复多次使用，添加多个文件；
2. git commit -m "explain what you do" // 必须双引号 // commit可以一次提交很多文件

git status告诉你有文件被修改过，
用git diff可以查看修改内容。

HEAD指向的版本就是当前版本，因此，Git允许我们在版本的历史之间穿梭，使用命令git reset --hard commit_id。

git log可以查看提交历史，以便确定要回退到哪个版本。

要重返未来，用git reflog查看命令历史，以便确定要回到未来的哪个版本。
